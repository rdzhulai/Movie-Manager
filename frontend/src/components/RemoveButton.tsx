const RemoveButton = () => {
  return (
    <button
      className="bg-red-700 hover:bg-red-600 movie-data-button"
      type="button"
    >
      Remove
    </button>
  );
};

export default RemoveButton;
